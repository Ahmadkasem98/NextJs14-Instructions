// to prevent error 404 when refresh drom link slot from notification archived when refresh

import Card from "../../../components/card";
import React from "react";

type Props = {};

const DefaultRevenu = (props: Props) => {
  return <Card>Revenue Page</Card>;
};

export default DefaultRevenu;
