// parallel Routes

import Card from "../../../components/card";
import React from "react";

type Props = {};

const Users = (props: Props) => {
  return <Card>Add Users</Card>;
};

export default Users;
